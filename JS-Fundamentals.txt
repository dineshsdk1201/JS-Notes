MODERN JAVASCRIPT CHEAT SHEET (UDemy Style)

Covers ES6+ concepts with real examples from your code


📌 INDEX (Quick Navigation)
1. JavaScript Fundamentals

Strict Mode
Variables (const, let)
Arrays
Objects
Functions (Methods)

2. ES6+ Object & Array Features

Enhanced Object Literals
Computed Property Names
Destructuring (Arrays & Objects)
Spread Operator (...)
Rest Pattern & Rest Parameters

3. Logical & Safety Operators

Short-Circuiting (||, &&)
Nullish Coalescing (??)
Logical Assignment Operators
Optional Chaining (?.)

4. Strings (Very Important)

String Creation & Access
String Methods (Case, Trim, Replace)
String Formatting (Pad, Repeat)

5. Data Structures

Sets
Maps
Maps vs Objects

6. Loops & Iteration

for...of loop
Looping Arrays, Objects, Maps

7. DOM & Events

DOM Creation
Event Handling

8. Practical Challenges (Applied JS)

Variable Conversion
Football App Challenges


🔹 1. STRICT MODE
JavaScript'use strict';Show more lines
✅ What it does

Prevents accidental global variables
Throws errors instead of failing silently

❌ Without strict mode
JavaScriptx = 10; // No errorShow more lines
✅ With strict mode
JavaScript'use strict';x = 10; // ❌ ReferenceErrorShow more lines
✅ Always use it at the top of your file

🔹 2. VARIABLES (const, let)
JavaScriptconst name = 'Jonas'; // Cannot be reassignedlet age = 30;        // Can be reassigned``Show more lines
✅ Use const by default
✅ Use let only when reassignment is needed
❌ Avoid var

🔹 3. ARRAYS
JavaScriptconst weekdays = ['mon', 'tue', 'wed', 'thu'];``Show more lines
✅ Ordered
✅ Zero-based index
JavaScriptconsole.log(weekdays[0]); // monShow more lines

🔹 4. OBJECTS
JavaScriptconst restaurant = {  name: 'Classico Italiano',  location: 'Italy',  categories: ['Italian', 'Pizza'],};``Show more lines
✅ Key–value pairs
✅ Best for structured data

🔹 5. FUNCTIONS (OBJECT METHODS)
JavaScriptorder(starterIndex, mainIndex) {  return [    this.starterMenu[starterIndex],    this.mainMenu[mainIndex]  ];}Show more lines
✅ this refers to the object
✅ Methods belong to objects

🔹 6. ENHANCED OBJECT LITERALS
JavaScriptconst openingHours = { fri: { open: 11, close: 23 } };const restaurant = {  openingHours,  order() {},};``Show more lines
✅ Cleaner syntax
✅ No repetition

🔹 7. COMPUTED PROPERTY NAMES
JavaScriptconst days = ['mon', 'tue', 'wed'];const hours = {  [days[0]]: { open: 9, close: 17 },};Show more lines
✅ Dynamic object keys
✅ Used with arrays & loops

🔹 8. DESTRUCTURING
✅ Array Destructuring
JavaScriptconst arr = [1, 2, 3];const [a, b] = arr;Show more lines
✅ Extract by position

✅ Object Destructuring
JavaScriptconst { name, categories } = restaurant;Show more lines
✅ Extract by key names

✅ Default Values
JavaScriptconst { menu = [] } = restaurant;Show more lines

🔹 9. SPREAD OPERATOR (...)
✅ Copy Arrays
JavaScriptconst newArr = [...oldArr];Show more lines
✅ Merge Arrays
JavaScriptconst menu = [...starters, ...mains];``Show more lines
✅ Copy Objects
JavaScriptconst restaurantCopy = { ...restaurant };Show more lines
✅ Prevents mutation
✅ Used heavily in React

🔹 10. REST PATTERN & PARAMETERS
✅ Rest in Destructuring
JavaScriptconst [a, b, ...others] = [1, 2, 3, 4];Show more lines
✅ Rest in Functions
JavaScriptfunction add(...numbers) {  let sum = 0;  for (const n of numbers) sum += n;  console.log(sum);}``Show more lines
JavaScriptadd(1, 2, 3); // 6Show more lines

🔹 11. SHORT-CIRCUITING
✅ OR Operator (||)
JavaScriptconst guests = restaurant.numGuests || 10;Show more lines
Returns first truthy value

✅ AND Operator (&&)
JavaScriptrestaurant.orderPizza && restaurant.orderPizza('mushroom');Show more lines
✅ Conditional execution without if

🔹 12. NULLISH COALESCING (??)
JavaScriptconst guests = restaurant.numGuests ?? 10;Show more lines
✅ Only checks:

null
undefined

❌ Does NOT treat 0 as false

🔹 13. LOGICAL ASSIGNMENT OPERATORS
JavaScriptrest.numGuests ??= 10;rest.owner &&= '<ANONYMOUS>';Show more lines
✅ Shorter
✅ Cleaner

🔹 14. OPTIONAL CHAINING (?.)
JavaScriptrestaurant.openingHours.mon?.open;Show more lines
✅ Prevents runtime crashes
✅ Works on:

Objects
Arrays
Methods


🔹 15. STRING BASICS
JavaScriptconst airline = 'TAP Air Portugal';Show more lines
JavaScriptconsole.log(airline[0]);  // Tconsole.log(airline.length);Show more lines

🔹 16. STRING METHODS
JavaScriptstr.toLowerCase();str.toUpperCase();str.trim();str.replace('door', 'gate');str.includes('Air');``Show more lines
✅ Used for:

Validation
Cleaning input
Formatting output


🔹 17. STRING FORMATTING
JavaScript'Jonas'.padStart(10, '*');'Hello'.repeat(3);``Show more lines
✅ UI formatting
✅ Logging clarity

🔹 18. SETS
JavaScriptconst orders = new Set(['Pizza', 'Pizza', 'Pasta']);Show more lines
✅ Automatically removes duplicates
✅ No indexes
JavaScriptorders.has('Pizza');Show more lines

🔹 19. MAPS
JavaScriptconst gameEvents = new Map([  [17, 'GOAL'],  [64, 'YELLOW CARD'],]);Show more lines
✅ Any key type
✅ Ordered
✅ Better than objects for dynamic data

🔹 20. MAPS vs OBJECTS

























FeatureObjectMapKey TypesStrings onlyAnyOrderNoYesPerformanceAverageBetter

🔹 21. for...of LOOP
JavaScriptfor (const item of menu) console.log(item);Show more lines
✅ Best for iterables
✅ Clean & readable

🔹 22. LOOPING OBJECTS
JavaScriptObject.keys(obj);Object.values(obj);Object.entries(obj);Show more lines
✅ Used for reporting
✅ Used for dynamic UI

🔹 23. DOM CREATION
JavaScriptdocument.body.append(document.createElement('textarea'));Show more lines
✅ DOM manipulation
✅ UI building

🔹 24. EVENTS
JavaScriptbutton.addEventListener('click', function () {  console.log('Clicked');});Show more lines
✅ Event-driven programming

🔹 25. REAL CODING CHALLENGE EXAMPLE
underscore_case ➜ camelCase
JavaScriptconst rows = text.split('\n');for (const [i, row] of rows.entries()) {  const [first, second] = row.toLowerCase().trim().split('_');  const output = `${first}${second[0].toUpperCase() + second.slice(1)}`;  console.log(output.padEnd(20) + '✅'.repeat(i + 1));}Show more lines
✅ Real‑world logic
✅ Interview-level question
 /////////////////////////////////////////////////////////////////////////


 ✅ JavaScript Concepts Cheat Sheet (With Simple Examples)

1️⃣ Strict Mode
Concept
Enables safer JavaScript by throwing errors for bad practices.
JavaScript'use strict';x = 10; // ❌ Error (must declare variable)Show more lines
✅ Prevents silent bugs
✅ Recommended in all projects

2️⃣ Variables
const
Used when value should not be reassigned.
JavaScriptconst pi = 3.14;Show more lines
let
Used when value can change.
JavaScriptlet count = 0;count++;``Show more lines

3️⃣ Data Types (Basics)
JavaScript// Numberlet age = 25;// Stringlet name = 'John';// Booleanlet isActive = true;// Undefinedlet value;// Nulllet data = null;``Show more lines

4️⃣ Template Literals
Concept
Easier string interpolation.
JavaScriptconst name = 'Alice';console.log(`Hello ${name}`);Show more lines
✅ Better than string concatenation

5️⃣ Objects
Concept
Group related data using key–value pairs.
JavaScriptconst user = {  name: 'John',  age: 30,  isAdmin: true};Show more lines
Access / Modify
JavaScriptuser.age = 31;user.city = 'Delhi';Show more lines

6️⃣ Arrays
Concept
Ordered collection of values.
JavaScriptconst numbers = [1, 2, 3, 4];Show more lines

7️⃣ Copying Data (Important Concept)
Copy Array (Shallow)
JavaScriptconst arr2 = arr1.slice();const arr3 = [...arr1];Show more lines
Why?
✅ Avoid unwanted mutation

8️⃣ Functions
Function Declaration
JavaScriptfunction greet() {  console.log('Hello');}Show more lines
Function Expression
JavaScriptconst greet = function () {  console.log('Hello');};``Show more lines
Arrow Function
JavaScriptconst greet = () => console.log('Hello');Show more lines

9️⃣ Callback Functions
Concept
Function passed into another function.
JavaScriptsetTimeout(() => {  console.log('Done');}, 1000);Show more lines
Used heavily in array methods and events.

🔟 Array Iteration Methods (CORE CONCEPTS)

✅ forEach – Do Something for Each Item
Used for side effects, not returning values.
JavaScript[1, 2, 3].forEach(num => {  console.log(num);});``Show more lines
❌ Does NOT return a new array

✅ map – Transform Array
JavaScriptconst doubled = [1, 2, 3].map(n => n * 2);// [2, 4, 6]``Show more lines
✅ Returns new array
✅ Same length

✅ filter – Select Items
JavaScriptconst positives = [-1, 2, -3, 4].filter(n => n > 0);// [2, 4]Show more lines
✅ Returns new array
✅ Length may change

✅ reduce – Combine Into One Value
JavaScriptconst sum = [1, 2, 3].reduce((acc, val) => acc + val, 0);// 6Show more lines
✅ Used for sum, average, max, counts

✅ find – First Match
JavaScript[5, 8, 12].find(n => n > 6);// 8``Show more lines
✅ Returns element
✅ Stops early

✅ some – Any Match?
JavaScript[1, 2, 3].some(n => n > 2);// trueShow more lines

✅ every – All Match?
JavaScript[2, 4, 6].every(n => n % 2 === 0);// true``Show more lines

1️⃣1️⃣ Method Chaining
Concept
Run multiple operations in a pipeline.
JavaScriptconst result = [1, -2, 3]  .filter(n => n > 0)  .map(n => n * 2)  .reduce((a, b) => a + b, 0);// 8Show more lines
✅ Clean
✅ Readable
✅ Modern JavaScript style

1️⃣2️⃣ Sorting
Concept
Sort values (mutates array).
JavaScriptnumbers.sort((a, b) => a - b);``Show more lines
✅ Safe way:
JavaScriptnumbers.slice().sort();Show more lines

1️⃣3️⃣ Flattening Arrays
flat
JavaScript[[1, 2], [3, 4]].flat();// [1,2,3,4]Show more lines
flatMap
JavaScriptarr.flatMap(x => x);Show more lines

1️⃣4️⃣ Sets (Remove Duplicates)
JavaScriptconst unique = [...new Set([1, 1, 2, 3])];// [1,2,3]Show more lines

1️⃣5️⃣ Maps (Key–Value Structure)
JavaScriptconst map = new Map();map.set('name', 'John');map.get('name');Show more lines

1️⃣6️⃣ Optional Chaining
Concept
Avoid runtime errors.
JavaScriptuser?.address?.city;Show more lines
✅ Safe access
✅ Returns undefined if missing

1️⃣7️⃣ Logical Operators
JavaScripta && b   // ANDa || b   // OR!a       // NOTShow more lines
Short‑circuit behavior:
JavaScripttrue && 'Hello'; // 'Hello'false || 'Hi';   // 'Hi'``Show more lines

1️⃣8️⃣ Ternary Operator
JavaScriptconst result = age >= 18 ? 'Adult' : 'Minor';Show more lines
✅ Short conditional rendering

1️⃣9️⃣ Modern Array Helpers
Non‑Mutating Methods
JavaScriptarr.toSorted();arr.toReversed();arr.with(1, 99);Show more lines
✅ Original array unchanged

2️⃣0️⃣ DOM Basics
Select
JavaScriptconst btn = document.querySelector('.btn');Show more lines
Update
JavaScriptbtn.textContent = 'Click';btn.style.color = 'red';Show more lines

2️⃣1️⃣ Events
JavaScriptbtn.addEventListener('click', () => {  console.log('Clicked');});Show more lines
Prevent default:
JavaScripte.preventDefault();``Show more lines

✅ FINAL MINI‑SUMMARY
Know This by Heart:

map → transform
filter → select
reduce → combine
find → first match
some → any
every → all
forEach → side effects


